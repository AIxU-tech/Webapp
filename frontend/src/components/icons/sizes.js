/**
 * Icon Size Constants
 *
 * Shared size scale for all SVG icon components.
 * Icons accept a `size` prop that maps to these values.
 * Pass `className` to override with arbitrary Tailwind classes.
 */
export const ICON_SIZES = {
  xs: 'h-3 w-3',
  sm: 'h-4 w-4',
  md: 'h-5 w-5',
  lg: 'h-6 w-6',
  xl: 'h-8 w-8',
};
